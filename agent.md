add user stories to the project as as new user-stories, call version 1.1-user-stories file:

as a developer, i want to get extract the AST from the gasd files once it is parsed (should be done after validation and it should be optional), so that i can use it to generate other files. this should come with a command line option to enable it. the output should be a json file. The output could be a file or printed to the console or one file per gasd or combined into one file. the default behavior should be to print to the console.

